# Highest published EXL3 precision that fits eight 16 GB V100s. The coded output head stays compressed; the trunk
# uses the qualified generic EXL3 spelling. The suffix distinguishes this branch from the 2.01 bpw RTX recipe.
model:
  huggingface: "turboderp/Laguna-S-2.1-exl3"
  revision: 3469659b2f9a1656805250880c6ea9760f9626ed
  task: generate

engine:
  llm:
    tensor_parallel_size: 1
    pipeline_parallel_size: 8
    gpu_memory_utilization: 0.84
    context_length: 262144
    max_concurrent_requests: 1
    vllm:
      image: "emmy-round2-laguna5-v100:f81bbe18"
      extra_args: >-
        --runner generate
        --dtype float16
        --generation-config vllm
        --hf-overrides '{"architectures":["EmmyGenModel"],"quantization_config":null}'
        --enable-auto-tool-choice
        --tool-call-parser poolside_v1
        --reasoning-parser poolside_v1
        --max-num-batched-tokens 16
        --enforce-eager
        --no-enable-prefix-caching
      extra_env:
        EMMY_FAST_MATH: "0"
        EMMY_PACK_DIR: "/opt/emmy/pack"
        EMMY_GEN_DECODE_BUCKET: "16"
        EMMY_GEN_PREFILL_CAPACITY: "16"
        EMMY_GEN_PREFILL_BUCKET: "0"
        EMMY_GEN_M1_TIER: "1"
        PYTORCH_ALLOC_CONF: "expandable_segments:True"
        PYTORCH_CUDA_ALLOC_CONF: "expandable_segments:True"

matrices:
  deploy.gpu: "NVIDIA Tesla V100 SXM2 16GB"
  deploy.gpu_count: 8
