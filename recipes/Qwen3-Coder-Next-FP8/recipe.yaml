tags:
  - best-effort

model:
  huggingface: "Qwen/Qwen3-Coder-Next-FP8"
  rationale: "Next-gen coder useful but Qwen3-Coder-30B AWQ covers the consumer tier."

engine:
  llm:
    tensor_parallel_size: 1
    pipeline_parallel_size: 1
    gpu_memory_utilization: 0.9
    context_length: 262144
    max_concurrent_requests: 32
    vllm:
      image: "vllm/vllm-openai:v0.17.0"

benchmark:
  max_concurrency: 32
  num_prompts: 80
  random_input_len: 4000
  random_output_len: 4000

matrices:
  deploy.gpu: "NVIDIA RTX PRO 6000 Blackwell Server Edition"
  deploy.gpu_count: 1
