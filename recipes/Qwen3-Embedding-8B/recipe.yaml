# Qwen3-Embedding-8B: stock vLLM pooling vs vLLM + emmy compiled-kernel plugin
# (emmy/serving — same OpenAI /v1/embeddings shell, different kernels).
#
# context_length is pinned to 4096 on BOTH engines for an apples-to-apples comparison —
# 4096 is emmy's dynamic-dim max (compiler/trace/dynamic.py DYNAMIC_DIM_MAX); the
# model natively supports 32k.
#
# The emmy variant compiles the whole model at engine start (~3 min first boot,
# faster after — EMMY_CUBIN_CACHE persists compiled cubins on the model volume).
# Stock vLLM ignores that env var, so it sits at the base level for both variants.
model:
  huggingface: "Qwen/Qwen3-Embedding-8B"
  task: embed

engine:
  llm:
    tensor_parallel_size: 1
    pipeline_parallel_size: 1
    # 8B fp16 weights ≈ 16 GB. On the 24 GB 4090 the emmy variant's cupy-held
    # weights are non-torch memory that vLLM's profiler subtracts from this budget —
    # utilization must clear (weights + overhead)/24 with headroom while leaving
    # vLLM a positive remainder (the attention-free plugin needs no KV cache).
    gpu_memory_utilization: 0.92
    context_length: 4096
    max_concurrent_requests: 32
    vllm:
      extra_env:
        EMMY_CUBIN_CACHE: "/hf_models/emmy-cubins"

benchmark:
  max_concurrency: 32
  num_prompts: 256
  random_input_len: 512   # overridden per regime by the matrix
  random_output_len: 1    # unused for task: embed (flag omitted from the bench command)

matrices:
  cross:
    deploy.gpu:
      - "NVIDIA GeForce RTX 5090"
      - "NVIDIA GeForce RTX 4090"
    deploy.gpu_count: 1
    benchmark.random_input_len: [32, 512]   # short-query + document regimes
    zip:
      engine.llm.vllm.image:
        - "vllm/vllm-openai:v0.22.1"
        # emmy compiled-kernel serving image (registers EmmyEmbedModel)
        - "cloudriftai/vllm-emmy:0.22.1-90c70d5e"
      engine.llm.vllm.extra_args:
        - "--runner pooling"
        - "--runner pooling --enforce-eager --hf-overrides '{\"architectures\":[\"EmmyEmbedModel\"]}'"
