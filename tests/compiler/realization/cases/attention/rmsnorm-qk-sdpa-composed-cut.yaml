# evidence: the sibling case rmsnorm-qk-sdpa-stat-b-cut cuts the clustered K operand cone alone and
# runs correct; the A cone cluster and the shared softmax-statistics cone are seams of the same
# kernel, so the composed pin row must realize all three cuts in one placement decision — the
# route that removes every redundant per-query recompute from the attention tree.
compute_cap:
- 12
- 0
programs:
- inputs: [q, k, v, qw, kw]
  outputs: [out]
  nodes:
  - id: k
    op: input
    outputs: [[k, f16, [1, 2, 8, 16]]]
  - id: kw
    op: input
    outputs: [[kw, f16, [16]]]
  - id: kn
    op: torch.rms_norm
    attrs: {eps: 1.0e-06}
    inputs: [k, kw]
    outputs: [[kn, f16, [1, 2, 8, 16]]]
  - id: q
    op: input
    outputs: [[q, f16, [1, 2, 8, 16]]]
  - id: qw
    op: input
    outputs: [[qw, f16, [16]]]
  - id: qn
    op: torch.rms_norm
    attrs: {eps: 1.0e-06}
    inputs: [q, qw]
    outputs: [[qn, f16, [1, 2, 8, 16]]]
  - id: v
    op: input
    outputs: [[v, f16, [1, 2, 8, 16]]]
  - id: out
    op: torch.sdpa
    attrs: {is_causal: true, sliding_window: null, scale: null}
    inputs: [qn, kn, v]
    outputs: [[out, f16, [1, 2, 8, 16]]]
configs:
- program: 0
  target:
    origins:
    - kn
    - out
    - qn
  realizations:
  - name: k_sdpa_rms_norm_reduce_dd476e.6741552db729
    bindings: {}
    pins:
      FAST_MATH: false
      PLACE@map.fold.a21: cut
      PLACE@map.fold.a.map.fold.fold.b1: cut
      PLACE@map.fold.a.map.fold.fold.a1: cut
    knobs:
      TILE: ''
      WORK: t16
      REDUCE: coop
      STAGE: ''
      RASTER: ''
    identity: 83e4f2a6fa484a4b2a50babe833fc9cf35021c1f551fcfabaf496658c24d055f
