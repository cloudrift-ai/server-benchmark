# evidence: realizes under the composed hand pin (`offered` passes). As evidence the route is read fork by
# fork on brand-new pieces: the statistic seam nested inside the K cone is spelled on the parent's tree and,
# after the K cut, lives on the K producer under another spelling, which no row of the parent can name — the
# case needs one receipt per piece.
# evidence: the sibling case rmsnorm-qk-sdpa-stat-b-cut cuts the normalized-K cone alone and runs
# correct; the normalized-Q cone and the K statistic nested inside the K cone are seams of the same
# kernel, so the composed pin row must realize all three cuts in one placement decision — the K
# cone's producer reading the statistic's workspace — the route that removes every redundant
# per-query recompute of the normalized operands from the attention tree.
compute_cap:
- 12
- 0
programs:
- inputs: [q, k, v, qw, kw]
  outputs: [out]
  nodes:
  - id: k
    op: input
    outputs: [[k, f16, [1, 2, 8, 16]]]
  - id: kw
    op: input
    outputs: [[kw, f16, [16]]]
  - id: kn
    op: torch.rms_norm
    attrs: {eps: 1.0e-06}
    inputs: [k, kw]
    outputs: [[kn, f16, [1, 2, 8, 16]]]
  - id: q
    op: input
    outputs: [[q, f16, [1, 2, 8, 16]]]
  - id: qw
    op: input
    outputs: [[qw, f16, [16]]]
  - id: qn
    op: torch.rms_norm
    attrs: {eps: 1.0e-06}
    inputs: [q, qw]
    outputs: [[qn, f16, [1, 2, 8, 16]]]
  - id: v
    op: input
    outputs: [[v, f16, [1, 2, 8, 16]]]
  - id: out
    op: torch.sdpa
    attrs: {is_causal: true, sliding_window: null, scale: null}
    inputs: [qn, kn, v]
    outputs: [[out, f16, [1, 2, 8, 16]]]
configs:
- program: 0
  target:
    origins:
    - kn
    - out
    - qn
  realizations:
  - name: k_sdpa_rms_norm_reduce_21cf2e.638613d46784
    bindings: {}
    pins:
      FAST_MATH: false
      PLACE@map.1/twist.1/inner.1/map: cut
      PLACE@map.1/twist.1/inner.2/map: cut
      PLACE@map.1/twist.1/inner.2/map.3/map.1/reduce: cut
    knobs:
      TILE: ''
      WORK: t16
      REDUCE: coop
      STAGE: ''
      RASTER: ''
    identity: 348b719eb684ba26b7f3575ca10fce4c72f1405e6217c2ae9c41b568d04b8848
