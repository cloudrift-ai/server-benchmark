# evidence: the same reduced q/k RMSNorm -> causal SDPA tree offers PLACE@map.fold.a.fold.b1 (the
# softmax-V arm's computed B operand, corpus case rmsnorm-gqa-b-cut) and cuts it correctly; the
# statistic arm's score contraction carries the alpha-equivalent computed K operand cone, closed
# against the key sweep's body by normalization, so its seam must offer and realize the same way.
compute_cap:
- 12
- 0
programs:
- inputs: [q, k, v, qw, kw]
  outputs: [out]
  nodes:
  - id: k
    op: input
    outputs: [[k, f16, [1, 2, 8, 16]]]
  - id: kw
    op: input
    outputs: [[kw, f16, [16]]]
  - id: kn
    op: torch.rms_norm
    attrs: {eps: 1.0e-06}
    inputs: [k, kw]
    outputs: [[kn, f16, [1, 2, 8, 16]]]
  - id: q
    op: input
    outputs: [[q, f16, [1, 2, 8, 16]]]
  - id: qw
    op: input
    outputs: [[qw, f16, [16]]]
  - id: qn
    op: torch.rms_norm
    attrs: {eps: 1.0e-06}
    inputs: [q, qw]
    outputs: [[qn, f16, [1, 2, 8, 16]]]
  - id: v
    op: input
    outputs: [[v, f16, [1, 2, 8, 16]]]
  - id: out
    op: torch.sdpa
    attrs: {is_causal: true, sliding_window: null, scale: null}
    inputs: [qn, kn, v]
    outputs: [[out, f16, [1, 2, 8, 16]]]
configs:
- program: 0
  target:
    origins:
    - kn
    - out
    - qn
  realizations:
  - name: k_sdpa_rms_norm_reduce_dd476e.6741552db729
    bindings: {}
    pins:
      FAST_MATH: false
      PLACE@map.fold.a.map.fold.fold.b1: cut
    knobs:
      TILE: ''
      WORK: t16
      REDUCE: coop
      STAGE: ''
      RASTER: ''
    identity: 83e4f2a6fa484a4b2a50babe833fc9cf35021c1f551fcfabaf496658c24d055f
