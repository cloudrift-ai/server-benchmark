# The recognized fused edge as ONE mma kernel whose statistic prologue rides the A cone —
# no separate norm-reduce producer. S=32 overhangs the 64-row warp tile, so this also
# exercises the masked static-M compute-fill: clamped A / statistic and a guarded store.
compute_cap:
- 12
- 0
programs:
- inputs: [x, wn, w]
  outputs: [y]
  nodes:
  - id: w
    op: input
    outputs: [[w, f16, [512, 128]]]
  - id: wn
    op: input
    outputs: [[wn, f16, [128]]]
  - id: x
    op: input
    outputs: [[x, f16, [1, 32, 128]]]
  - id: xn
    op: torch.rms_norm
    attrs: {eps: 1.0e-06}
    inputs: [x, wn]
    outputs: [[xn, f16, [1, 32, 128]]]
  - id: y
    op: torch.linear
    attrs: {has_bias: false}
    inputs: [xn, w]
    outputs: [[y, f16, [1, 32, 512]]]
configs:
- program: 0
  target:
    origins:
    - xn
    - y
  realizations:
  - name: k_rms_norm_linear_reduce_dee3f2.bbeb54a322f3
    bindings: {}
    pins:
      FAST_MATH: false
    knobs:
      TILE: mma_m16n8k16_f16_f32/f2x2/k2
      WORK: w2x2
    identity: 553b4d18697c2e63412b4d4a4c77b5aa71fef69b6fd3c0d9c4fb58dcfa3c9eba
