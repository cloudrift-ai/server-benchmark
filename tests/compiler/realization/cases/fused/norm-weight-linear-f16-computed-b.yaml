# evidence: the sibling norm-linear cases realize the same mma tier with the norm on the A side;
# with the norm on the WEIGHT the contraction takes the computed-B smem compute fill, whose cone
# re-binds the contraction axis name for its own sum-of-squares statistic. Guards the fill's
# hygienic σ: a blind substitution rewired that nested reduce onto the slab coordinate, summing
# one element 128 times (wrong answer, not a refusal).
compute_cap:
- 12
- 0
programs:
- inputs: [x0, x1, x2]
  outputs: [linear]
  nodes:
  - id: x0
    op: input
    outputs: [[x0, f16, [64, 128]]]
  - id: x1
    op: input
    outputs: [[x1, f16, [64, 128]]]
  - id: x2
    op: input
    outputs: [[x2, f16, [128]]]
  - id: rms_norm
    op: torch.rms_norm
    attrs: {eps: 1.0e-06}
    inputs: [x1, x2]
    outputs: [[rms_norm, f16, [64, 128]]]
  - id: linear
    op: torch.linear
    attrs: {has_bias: false}
    inputs: [x0, rms_norm]
    outputs: [[linear, f16, [64, 64]]]
configs:
- program: 0
  target:
    origins:
    - linear
    - rms_norm
  realizations:
  - name: k_rms_norm_linear_reduce_1ede22.1b8e6f4b896e
    bindings: {}
    pins:
      FAST_MATH: false
    knobs:
      WORK: w1x1
      TILE: mma_m16n8k16_f16_f32/f1x4/k8
    identity: 59b172acf0f5f922e023034c9b0266e49b117f977ba666e861ab78b44fc49095
