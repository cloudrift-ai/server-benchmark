# One half of the contraction reuse the warp-flash materializer rides: a bilinear fold placed
# under LEADING batch grid axes, tiled onto mma.sync by the shared contraction codegen rather
# than by anything attention-specific.
#
# The output contraction: the reduce runs over the key axis and V is stored [kv, d], so B is
# canonical and drains through the non-transposing gmem fragment helper.
#
# One batch, four heads, a 128-long key axis.
compute_cap:
- 12
- 0
programs:
- inputs: [p, v]
  outputs: [c]
  nodes:
  - id: p
    op: input
    outputs: [[p, f16, [1, 4, 128, 128]]]
  - id: v
    op: input
    outputs: [[v, f16, [1, 4, 128, 128]]]
  - id: c
    op: torch.matmul
    attrs: {has_bias: false}
    inputs: [p, v]
    outputs: [[c, f32, [1, 4, 128, 128]]]
configs:
- program: 0
  target:
    origins:
    - c
  realizations:
  - name: k_matmul_9d9788.9f55b5943809
    bindings: {}
    pins:
      FAST_MATH: false
    knobs:
      TILE: mma_m16n8k16_f16_f32/f4x8/k2
      WORK: w2x2
    identity: 0313498a662f8ea528b07b7fae399e7b09640dc5450184f53d54900d8bb0b2e9
