# One geometry of the scalar contraction's free-axis output tile (the TILE codec) on the
# file's square f32 base shape. The static column only: a symbolic M would run at its 512
# hint, which is an exact cover of every one of these tiles and so asserts nothing the
# static row does not already assert.
#
# The per-cell tier: one thread per output cell, no register replication and no unrolled
# inner reduce. The floor every other tile in this family is measured against.
compute_cap:
- 12
- 0
programs:
- inputs: [a, b]
  outputs: [c]
  nodes:
  - id: a
    op: input
    outputs: [[a, f32, [1, 64, 64]]]
  - id: b
    op: input
    outputs: [[b, f32, [64, 64]]]
  - id: c
    op: torch.matmul
    attrs: {has_bias: false}
    inputs: [a, b]
    outputs: [[c, f32, [1, 64, 64]]]
configs:
- program: 0
  target:
    origins:
    - c
  realizations:
  - name: k_matmul_f40431.ff29a7fdbea1
    bindings: {}
    pins:
      FAST_MATH: false
    knobs:
      TILE: ''
      WORK: ''
    identity: beca40c263c7e0ebf2c698b3ace7089a496f27a755437a46a861021e103c9f5b
