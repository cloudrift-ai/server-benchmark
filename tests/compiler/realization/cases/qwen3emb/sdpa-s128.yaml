# Qwen3-Embedding-0.6B decoder layer 0, kernels 6 and 7: causal GQA attention.
# hidden=1024 heads=16 kv_heads=8 head_dim=128, fp32, seq 128. One of the twelve layer-0
# kernels the perf suite curated before the corpus became its case source.
#
# The case preserves the fused attention program; its schedule is selected afresh through exact
# node and operand-edge sites.
compute_cap:
- 12
- 0
programs:
- inputs: [q, k, v]
  outputs: [o]
  nodes:
  - id: k
    op: input
    outputs: [[k, f32, [1, 8, 128, 128]]]
  - id: q
    op: input
    outputs: [[q, f32, [1, 16, 128, 128]]]
  - id: v
    op: input
    outputs: [[v, f32, [1, 8, 128, 128]]]
  - id: o
    op: torch.sdpa
    attrs: {is_causal: true, sliding_window: null, scale: null}
    inputs: [q, k, v]
    outputs: [[o, f32, [1, 16, 128, 128]]]
configs:
- program: 0
  target:
    origins:
    - o
  realizations:
  - name: k_sdpa_a141d6.d9cbca6c4928
    bindings: {}
    pins:
      FAST_MATH: false
    knobs: {}
    identity: 50542419d1b2fa054cfb754713f2d91adb1253ce419cb3e3dd83385552802d1d
