# The carrier-generic cooperative combine at one cell of its matrix: a degenerate Accum carrier over the maximum monoid,
# partitioned as the 4-warp hierarchical shuffle-then-smem-tree (coop at a 128-thread block).
#
# Every carrier folds through the SAME combine and differs only in its state, so one
# partition kind breaking on a carrier it was not tuned on is exactly what the matrix
# catches. The reduce axis is 1024 so the 128-thread row reaches the cross-warp tree.
compute_cap:
- 12
- 0
programs:
- inputs: [x]
  outputs: [amax]
  nodes:
  - id: x
    op: input
    outputs: [[x, f32, [4, 1024]]]
  - id: amax
    op: tensor.reduce
    attrs: {op: {__elementwise__: amax}, axis: 1}
    inputs: [x]
    outputs: [[amax, f32, [4, 1]]]
configs:
- program: 0
  target:
    origins:
    - amax
  realizations:
  - name: k_amax_reduce.1605bfd0df97
    bindings: {}
    pins:
      FAST_MATH: false
    knobs:
      REDUCE: coop
      WORK: t128
    identity: 1d17c20cacaaa3db585799d9b15b78740fc558732acc3e7466cd689f07e0f13f
