# The transposed cooperative band on a k-major matvec: 32 lanes sweep the OUTPUT axis over a
# ceil(N/32) grid, here N=512.
#
# 32 divides the swept extent, so the sweep tiles exactly and no overhang guard is emitted.
compute_cap:
- 12
- 0
programs:
- inputs: [x0, x1]
  outputs: [linear]
  nodes:
  - id: x0
    op: input
    outputs: [[x0, f32, [1, 256]]]
  - id: x1
    op: input
    outputs: [[x1, f32, [512, 256]]]
  - id: linear
    op: torch.linear
    attrs: {has_bias: false}
    inputs: [x0, x1]
    outputs: [[linear, f32, [1, 512]]]
configs:
- program: 0
  target:
    origins:
    - linear
  realizations:
  - name: k_linear_fbe2b7.b487b7f8bd65
    bindings: {}
    pins:
      FAST_MATH: false
    knobs:
      WORK: t128
      REDUCE: coop-t
    identity: 2217b7f5011b3b4d08b1e2250e30ec5c6a3ff8325fa553369ea4603e22f3fb15
