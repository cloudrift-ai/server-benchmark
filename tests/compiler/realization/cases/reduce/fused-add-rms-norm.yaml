# A residual add feeding an rms_norm with no other consumer folds into the norm's sweep — the
# serving decode residual chain, and no standalone pointwise add kernel survives it.
#
# Greedy's own row is authored here because the test this replaces pinned nothing.
compute_cap:
- 12
- 0
programs:
- inputs: [x, r, w]
  outputs: [n]
  nodes:
  - id: r
    op: input
    outputs: [[r, f32, [8, 256]]]
  - id: w
    op: input
    outputs: [[w, f32, [256]]]
  - id: x
    op: input
    outputs: [[x, f32, [8, 256]]]
  - id: h
    op: tensor.elementwise
    attrs: {op: {__elementwise__: add}}
    inputs: [x, r]
    outputs: [[h, f32, [8, 256]]]
  - id: n
    op: torch.rms_norm
    attrs: {eps: 1.0e-06}
    inputs: [h, w]
    outputs: [[n, f32, [8, 256]]]
configs:
- program: 0
  target:
    origins:
    - h
    - n
  realizations:
  - name: k_rms_norm_e7c37c.1fbd2b41aff2
    bindings: {}
    pins:
      FAST_MATH: false
    knobs:
      TILE: ''
      REDUCE: coop
      WORK: t128
    identity: 7bc7a169f9d4039acc89ff7228a1392f24b3732fcb9a94b7bc01376be176c15c
