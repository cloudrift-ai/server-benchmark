# The two-pass softmax (row max, then the exp sum, then normalize) rewritten into ONE
# streaming pass over the (m, d) monoid — three reads of the row collapse to two.
#
# The schedule here is the one greedy picks; the test this replaces pinned nothing at all,
# and a case must author the row it expects, so the greedy row is written out. Its
# signature in the emitted kernel is the dissolved merge's rescale temps.
compute_cap:
- 12
- 0
programs:
- inputs: [x]
  outputs: [softmax]
  nodes:
  - id: softmax_c1
    op: constant
    attrs:
      name: softmax_c1
      value: -1.0
      context_value: null
      load_ops: {__tuple__: []}
      source_path: null
      source_parts: {__tuple__: []}
      source_shape: null
      source_dtype: null
      source_graph: null
    outputs: [[softmax_c1, f32, [1]]]
  - id: x
    op: input
    outputs: [[x, f32, [4, 128]]]
  - id: softmax
    op: torch.softmax
    attrs: {axis: -1}
    inputs: [x]
    outputs: [[softmax, f32, [4, 128]]]
configs:
- program: 0
  target:
    origins:
    - softmax
  realizations:
  - name: k_softmax_963fa6.ba10260fb856
    bindings: {}
    pins:
      FAST_MATH: false
    knobs:
      TILE: ''
      REDUCE: coop
      WORK: t128
    identity: d0278cc08c9dfe5e0f77c36289b1563266095237d806c18370c07e4084d7fba6
